package com.Electroinova.inovinfo.domain.usecase

/**
 * Casos de uso — pendientes de implementación.
 *
 * Se crearán use cases para:
 *  - GuardarVisitaUseCase     (Room local + sync a Sheets)
 *  - GetVisitasHoyUseCase     (HistorialScreen)
 *  - GetPendientesUseCase     (PendientesScreen)
 *  - GetUnidadInfoUseCase     (busca sede por número de unidad)
 *  - RedactarObservacionUseCase (invoca Gemini API)
 *  - SyncWithSheetsUseCase    (sincronización batch)
 */

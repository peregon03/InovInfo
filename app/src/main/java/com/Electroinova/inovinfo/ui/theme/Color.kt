package com.Electroinova.inovinfo.ui.theme

import androidx.compose.ui.graphics.Color

// ── Paleta de marca Electroinova Soluciones ──────────────────────────────────
val ElectroNavyBlue      = Color(0xFF1A3A5C)   // Primario — azul marino oscuro
val ElectroNavyBlueLight = Color(0xFF2D5F96)   // Variante clara del primario
val ElectroNavyBlueDark  = Color(0xFF0F2540)   // Variante oscura del primario

val ElectroGold          = Color(0xFFC9A84C)   // Secundario — dorado
val ElectroGoldLight     = Color(0xFFDFC170)   // Variante clara del dorado
val ElectroGoldDark      = Color(0xFF9E7E2E)   // Variante oscura del dorado

val ElectroBackground    = Color(0xFFFFFFFF)   // Fondo principal
val ElectroSurface       = Color(0xFFF5F7FA)   // Superficie / cards
val ElectroError         = Color(0xFFE24B4A)   // Error

// On-colors (texto/íconos sobre cada color de fondo)
val ElectroOnPrimary     = Color(0xFFFFFFFF)
val ElectroOnSecondary   = Color(0xFF1A1A1A)
val ElectroOnBackground  = Color(0xFF1A1A1A)
val ElectroOnSurface     = Color(0xFF2C2C2C)
val ElectroOnError       = Color(0xFFFFFFFF)
val ElectroOnSurfaceVar  = Color(0xFF5A5E6B)   // Texto secundario / subtítulos
